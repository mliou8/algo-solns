var str = "Sometimes (when I nest them (my parentheticals) too much (like this (and this))) they get confusing."

//Given a string with nested parentheticals, return the matching one for that parens.

function returnMatchingParens() {

}
